/**
 * `Middle` takes an Array and returns the Middle element according to the
 * given ordering function.
 * @params {Array} Array of elements
 * @params {Function} test Test function that returns true when a is smalle than b
 * @returns {any} The element at the center{Middle Position} in the input array
 */
function Middle(arr, test) {
  // your code here
}

module.exports = Middle;
